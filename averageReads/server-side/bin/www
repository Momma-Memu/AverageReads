#!/usr/bin/env node
const app = require('../app');

const { port } = require('../config');
const db = require('../db/models');

db.sequelize.authenticate()
    .then(() => {
        // If connection to the database succeeds:
        console.log('Database connection success! Sequelize is ready to use...');

        // Start listening for connections.
        app.listen(port, () => console.log(`Listening on port ${port}...`));
    })
    .catch((err) => {
        // If connection to the database fails:
        console.log('Database connection failure.');
        console.error(err);
    });
<<<<<<< HEAD:averageReads/bin/www
=======

// app.listen(port, () => console.log(`Listening on port ${port}...`));
// ^ caused error when listen was called a 2nd time
>>>>>>> 314ff2d8a2d235646d9b3f73d31d849fb9e0a593:averageReads/server-side/bin/www
