<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet", type="text/css" href="/style.css")
        link(rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/bulma@0.9.0/css/bulma.min.css")
        title Book
    body
        article(class="media")
            figure(class="media-left")

            div(class="media-content")
                div(class="content")
                p.title.pt-2 Reviews
                div(id="review")
                    for comment in comments
                        p
                            strong.pr-2=comment.User.username
                            small.pr-2=comment.User.email
                            small=comment.createdAt.getFullYear()
                            <br>
                            p.pb-3=comment.message
