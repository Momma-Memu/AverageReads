<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        script(src="js/index.js" type="module")
        link(rel="stylesheet", type="text/css" href="/style.css")
        link(rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/bulma@0.9.0/css/bulma.min.css")
        title Lost? | Averagereads
    body
        nav.navbar.navigations(role="navigation" aria-label="man navigation")
            div.navbar-brand
                a.navbar-item
                    img(src="/images/logo-small.png"  width="120" height="42" id="avgreadsimages")
            script(src="js/navbar.js")
        section(class="hero is-medium is-primary is-bold")
        div(class="hero-body")
            div(class="container")
            h1(class="title")
                Primary Sorry, we couldn't locate the page you were looking for...
            h2(class="subtitle")
                Primary Have an Average day!
        div(class="columns is-mobile is-centered")
            div(class="column is-half")
                img(src=`../images/confused-kid.jpg` class="")

        include footer.pug
